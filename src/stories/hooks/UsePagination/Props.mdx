import { Meta, Controls, Markdown } from '@storybook/blocks';
import * as UsePaginationStories from './UsePagination.stories';
import UsePaginationReturn from './UsePaginationReturn.md?raw'

<Meta of={UsePaginationStories} name='Props' />

# usePagination
Hook takes pagination config options as a parameter and return pagination state and controls.

## Parameters
<Controls />

## Returns

<Markdown>{UsePaginationReturn}</Markdown>
